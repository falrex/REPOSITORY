<h1>Listing publishquizzes</h1>

<table>
  <tr>
  </tr>

<% @publishquizzes.each do |publishquiz| %>
  <tr>
    <td><%= link_to 'Show', publishquiz %></td>
    <td><%= link_to 'Edit', edit_publishquiz_path(publishquiz) %></td>
    <td><%= link_to 'Destroy', publishquiz, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New publishquiz', new_publishquiz_path %>