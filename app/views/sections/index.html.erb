<h1><%= flash[:subjectname] %></h1>
<p><%= link_to 'Home', :controller=>"teacherpage" %></p>

<table>
  <tr>
    <th>Section</th>
    <th>Time</th>
    <th>Day</th>
  </tr>

<% @sections.each do |section| %>
  <tr>
    <td><%=h section.section %></td>
    <td><%=h section.time %></td>
    <td><%=h section.day %></td>
    <td><%= link_to 'Show', section %></td>
    <td><%= link_to 'Add Quiz', :controller=>"quizzes", :action=>"new",:section=>section%></td>
    <td><%= link_to 'Edit', edit_section_path(section) %></td>
    <td><%= link_to 'Destroy', section, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New section', new_section_path %>